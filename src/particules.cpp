#include"tower_game.hpp"
#include "particules.hpp"



void AddHealingParticules(GameData& data, const float2& pos)
{
	/*
	float2 center = {450,450};
	float2* points = new float2 (600,600 );

	float2 croix1 = { center.x - 20 ,center.x - 20 };

	float value = 9.f;
	float random[2];

	for (int i = 0; i < 2; i++)
	{
		random[i] = Calc::randomFloat(100, 200);
	}

	data.dl->AddRectFilled(croix1, float2(croix1.x, croix1.y - 7.5f), IM_COL32(255, 255, 0, 1 + (random[0] + data.deltatime)), 2, 0);
	data.dl->AddRectFilled(croix1, float2(croix1.x, croix1.y + 7.5f), IM_COL32(255, 255, 0, 1 + (random[1] + data.deltatime)), 2, 0);
	data.dl->AddRectFilled(float2(croix1.x + 7.5f, croix1.y), croix1, IM_COL32(255, 255, 0, 1 + (random[0] + data.deltatime)), 2, 0);
	data.dl->AddRectFilled(float2(croix1.x - 7.5f, croix1.y), croix1, IM_COL32(255, 255, 0, 1 + (random[0] + data.deltatime)), 2, 0);


	data.dl->AddCircle(center, 5.f, IM_COL32(255, 255, 0, 255), 23, 1);







	float2 d = *points - 20;

	*points -= normaliseVector(d) * (10 * data.deltatime);


	std::cout << points->x << " , " << points->y << std::endl;

	data.dl->AddRectFilled(*points, float2(points->x, points->y - 7.5f), IM_COL32(255, 255, 0, 1 + (random[0] + data.deltatime)), 2, 0);
	data.dl->AddRectFilled(*points, float2(points->x, points->y + 7.5f), IM_COL32(255, 255, 0, 1 + (random[1] + data.deltatime)), 2, 0);
	data.dl->AddRectFilled(float2(points->x + 7.5f, points->y), *points, IM_COL32(255, 255, 0, 1 + (random[0] + data.deltatime)), 2, 0);
	data.dl->AddRectFilled(float2(points->x - 7.5f, points->y), *points, IM_COL32(255, 255, 0, 1 + (random[1] + data.deltatime)), 2, 0);
	*/
}